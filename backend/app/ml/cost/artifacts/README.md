# Cost model artifacts

Drop your trained files here:

- `model.pkl` — joblib-serialized estimator (XGBoost, RandomForest, LightGBM, sklearn Pipeline)
- `preprocessor.pkl` — fitted preprocessor / ColumnTransformer
- `feature_config.json` — ordered feature names

See the root README section **How to integrate the real cost model**.
