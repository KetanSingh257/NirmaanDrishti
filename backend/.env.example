# Copy to .env and adjust.

# SQLite (default) — file is created next to this folder.
DATABASE_URL=sqlite:///./nirmaandrishti.db

# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://nirmaan:nirmaan@localhost:5432/nirmaandrishti

CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ENVIRONMENT=development
DEBUG=true

# Optional overrides for trained model artifacts
# COST_MODEL_PATH=app/ml/cost/artifacts/model.pkl
# COST_PREPROCESSOR_PATH=app/ml/cost/artifacts/preprocessor.pkl
# COST_FEATURE_CONFIG_PATH=app/ml/cost/artifacts/feature_config.json
# TIME_MODEL_PATH=app/ml/time/artifacts/model.pkl
